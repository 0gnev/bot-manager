{
  "$comment": [
    "OpenClaw AI gateway configuration.",
    "",
    "TELEGRAM_BOT_TOKEN_OWNER — tutor/owner bot; only whitelisted users (TUTOR_CHAT_ID) can interact.",
    "This token is used by OpenClaw for its Telegram channel AND by Bridge for sending escalation notices.",
    "",
    "The student-facing bot (TELEGRAM_BOT_TOKEN_STUDENT) is NOT configured here —",
    "it is polled directly by Bridge and is open to all users."
  ],
  "agents": {
    "defaults": {
      "model": {
        "primary": "openai/gpt-5-mini"
      },
      "compaction": {
        "mode": "safeguard"
      },
      "maxConcurrent": 4,
      "subagents": {
        "maxConcurrent": 8
      }
    }
  },
  "commands": {
    "native": "auto",
    "nativeSkills": "auto",
    "restart": true,
    "ownerDisplay": "raw"
  },
  "channels": {
    "telegram": {
      "enabled": false,
      "dmPolicy": "allowlist",
      "botToken": "${TELEGRAM_BOT_TOKEN_OWNER}",
      "allowFrom": ["tg:${TUTOR_CHAT_ID}"],
      "groupPolicy": "allowlist",
      "streaming": "block"
    }
  },
  "gateway": {
    "port": 18789,
    "bind": "lan",
    "controlUi": {
      "allowedOrigins": [
        "http://localhost:18789",
        "http://127.0.0.1:18789",
        "http://127.0.0.1:3000",
        "http://localhost:3000"
      ]
    },
    "auth": {
      "mode": "token",
      "token": "${GATEWAY_AUTH_TOKEN}"
    },
    "http": {
      "endpoints": {
        "chatCompletions": {
          "enabled": true
        }
      }
    }
  }
}
